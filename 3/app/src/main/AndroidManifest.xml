<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a3">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".MessageShow"></activity>
        <activity android:name=".ModifyEmail" />
        <activity android:name=".ModifyPwd" />
        <activity android:name=".ChangePwdByMail" />
        <activity android:name=".verify" />
        <activity android:name=".sendmail" />
        <activity android:name=".log_in" />
        <activity android:name=".beforelog">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".trashlist" />
        <activity android:name=".setting_list" />
    </application>

</manifest>